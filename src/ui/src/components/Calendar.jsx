import React from 'react';

export default function Calendar() {
  return (
    <section className="card">
      <h2>Calendar</h2>
      <p>Basic calendar view placeholder (we’ll integrate sessions + invites next).</p>
    </section>
  );
}
