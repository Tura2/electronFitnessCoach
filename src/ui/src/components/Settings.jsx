import React from 'react';

export default function Settings() {
  return (
    <section className="card">
      <h2>Settings</h2>
      <p>Coach profile, SMTP, reminders, preferences — coming next.</p>
    </section>
  );
}
